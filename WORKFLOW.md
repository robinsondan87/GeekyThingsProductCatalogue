# Workflow

Quick checklist for changes:

- Make changes.
- Update `CHANGELOG.md` with a brief summary.
- Verify status: `git status -sb`.
- Stage and commit: `git add .` then `git commit -m "Describe change"`.
- Push: `git push`.
